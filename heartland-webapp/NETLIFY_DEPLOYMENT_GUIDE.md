# 🚀 Netlify 部署指南

## 步骤 1：登录 Netlify

1. 访问：https://app.netlify.com/
2. 使用 GitHub 账号登录（推荐）
   - 点击 "Sign up" 或 "Log in"
   - 选择 "GitHub" 登录
   - 授权 Netlify 访问你的 GitHub

---

## 步骤 2：导入项目

1. 点击 **"Add new site"** 按钮
2. 选择 **"Import an existing project"**
3. 选择 **"Deploy with GitHub"**
4. 找到并选择你的仓库：
   ```
   tfiufiuyfiuf/heartland-webapp
   ```

---

## 步骤 3：配置部署设置

在配置页面，设置如下：

### 基本设置
- **Branch to deploy**: `main`
- **Base directory**: 留空（不填）
- **Build command**: 留空（不填）
- **Publish directory**: `.` （填一个点）

### 高级设置（可选）
不需要修改，保持默认即可。

---

## 步骤 4：部署

1. 点击 **"Deploy site"** 按钮
2. 等待 1-2 分钟，Netlify 会自动部署
3. 部署成功后，会显示一个随机域名，类似：
   ```
   https://your-site-name-abc123.netlify.app
   ```

---

## 步骤 5：测试

### 访问你的网站
```
https://你的域名.netlify.app/student-login.html
```

### 测试这些页面
- `https://你的域名.netlify.app/index.html`
- `https://你的域名.netlify.app/student-login.html`
- `https://你的域名.netlify.app/frontend/config.js`
- `https://你的域名.netlify.app/frontend/api.js`

所有文件都应该能正常访问！

---

## 步骤 6：自定义域名（可选）

如果你有自己的域名：

1. 在 Netlify 项目中，点击 **"Domain settings"**
2. 点击 **"Add custom domain"**
3. 输入你的域名（如 `heartland.com`）
4. 按照提示配置 DNS 记录

---

## 🎯 Netlify 的优势

相比 Vercel，Netlify 对静态站点的支持更好：
- ✅ 自动检测静态文件
- ✅ 不需要复杂的配置
- ✅ 部署日志更清晰
- ✅ 即时预览功能
- ✅ 免费的 SSL 证书

---

## 🔧 如果遇到问题

### 问题 1：部署后 404
- 检查 **Publish directory** 是否设置为 `.`
- 检查 **Base directory** 是否为空

### 问题 2：找不到仓库
- 确保你已授权 Netlify 访问 GitHub
- 在 GitHub 设置中检查 Netlify 的访问权限

### 问题 3：部署失败
- 查看部署日志
- 确保 `netlify.toml` 文件已提交到 GitHub

---

## 📝 当前配置

项目中已包含 `netlify.toml` 配置文件：
```toml
[build]
  publish = "."
  command = ""
```

这告诉 Netlify：
- 发布当前目录的所有文件
- 不需要执行任何构建命令

---

## 🚀 立即开始

1. 打开浏览器
2. 访问：https://app.netlify.com/
3. 按照上面的步骤操作
4. 5 分钟内完成部署！

---

**Netlify 比 Vercel 简单得多，应该不会有问题！**

